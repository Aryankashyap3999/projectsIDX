# Public URL of ProjectService (Socket.IO / editor + terminal). Use wss:// host in prod.
VITE_BACKEND_URL=http://localhost:3001

# Public URL of userService REST API (auth, projects). The app appends /api.
VITE_BACKEND_API_URL=http://localhost:3000
