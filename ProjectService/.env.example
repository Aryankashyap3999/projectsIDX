# Port the service listens on (Express + Socket.IO).
PORT=3001

# Command used to boot a React project inside its sandbox container.
REACT_PROJECT_COMMAND=npm run dev

# Allowed CORS origins (comma-separated). Leave unset in dev to allow all.
# e.g. https://your-frontend.vercel.app
CORS_ORIGIN=
